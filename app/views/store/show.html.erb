

<div>
  <div class="col-md-4">
    <span class="Store-show-img"><%= image_tag @product.image_url %></span>
    <legend>
      <p aling="center"><span>Price: </span><%= number_to_currency(@product.price)%></p>

    </legend>
  </div>

  <div class="col-lg-8">
    <div class="row">
      <div class="page-header">
        <%= link_to store_url, :class => 'btn btn-info' do%>
            <span class="glyphicon glyphicon-list-alt"></span>
            Back
        <% end %>
        <h1><b><%= @product.title %></b></h1>

      </div>

      <p align="justify"><%= @product.description %></p>


    </div>
  </div>
</div>
